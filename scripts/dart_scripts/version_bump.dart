
import 'package:flutter/material.dart';

class VersionBump extends StatelessWidget {
  const VersionBump({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
